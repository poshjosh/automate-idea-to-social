# [llm|browser|blog|generic]  llm is not yet supported
agent-type: generic
# Optional [generate-video|generate-text|post|test|custom|unlisted]
agent-tags: custom
sort-order: 1
stages:
  translate:
    stage-items:
      translate:
        actions: translate $TEXT_FILE $INPUT_LANGUAGE_CODE $LANGUAGE_CODES
